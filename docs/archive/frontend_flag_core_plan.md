# Frontend Flag Core Modularization Plan

## Summary

This plan starts frontend modularization with the area that changes most often: llama.cpp flag definitions, flag state, launch argument generation, and Configure tab flag rendering.

The goal is to make flag additions, removals, renames, default changes, and option updates easier to maintain while preserving the UI state sync rule from `AGENTS.md`.

Primary rule: Configure, Quick Launch, Chat sampler controls, presets, and command preview must continue to read and write through one shared flag state path.

## Phase 0: Baseline And Guardrails

Status: Complete

- [x] Record current implementation baseline before extraction:
  - `ui/js/flags.js` defines all supported flag input types: bool, int, float, text, path, enum, and multi_enum.
  - `ui/js/app.js` currently owns shared state and launch behavior through `currentTool`, `flagValues`, `setFlagValue()`, `setPathFlagValue()`, `setMultipleFlagValues()`, `collectFlagValues()`, `applyFlagValues()`, `updateCommandPreview()`, and `getLaunchArgs()`.
  - `syncUiAfterSharedStateChange()` currently refreshes Configure inputs, command preview, and Chat sampler UI after shared state changes.
  - Configure rendering currently lives in `renderFlags()`, `createSubmenuBlock()`, `createFlagRow()`, and `restoreFlagInputs()`.
  - Quick Launch writes model, context, GPU, template, sampler, and metrics controls through the shared setter path.
  - Chat sampler sliders write through `setFlagValue()`.
  - Preset load applies state through `applyFlagValues()`, and preset save/export reads through `collectFlagValues()` via the preset data helpers.
  - Command preview is generated by `getLaunchArgs()` from shared `flagValues`.
- [x] Add or document a manual smoke checklist for the workflows above.
- [x] Keep the first implementation behavior-neutral:
  - no framework migration
  - no `type="module"` conversion
  - no UI redesign
  - no intentional launch argument changes
- [x] Run manual browser smoke checklist before Phase 1 extraction. Completed with notes below.

### Phase 0 Smoke Checklist

Run this before and after each Phase 1 extraction step.

- [x] Configure renders a representative bool flag, int flag, float flag, text flag, path flag, enum flag, and multi_enum flag.
- [x] Configure search filters flags, shows matching submenu items, and shows the empty-results message for a nonsense query.
- [x] Configure Expand All opens all visible categories and submenus.
- [x] Configure Collapse All closes all categories and submenus.
- [x] Changing Configure model updates Quick Launch model.
- [x] Changing Quick Launch model updates Configure model and command preview.
- [x] Changing Quick Launch context updates Configure `ctx_size`, linked `fit_ctx` when applicable, and command preview.
- [x] Changing Configure `ctx_size` updates Quick Launch context.
- [x] Changing Quick Launch GPU mode/custom layers updates Configure `gpu_layers` and command preview.
- [x] Changing Configure `gpu_layers` updates Quick Launch GPU controls.
- [x] Changing Configure chat template updates Quick Launch template pack and summary.
- [x] Changing Quick Launch template pack updates Configure `chat_template` / `chat_template_custom` and command preview.
- [x] Changing Quick Launch sampler fields updates Configure sampler flags and Chat sampler controls.
- [x] Changing Chat sampler sliders updates Configure sampler flags and Quick Launch sampler fields.
- [x] Toggling Quick Launch metrics updates Configure `metrics` and command preview.
- [x] Toggling Configure `metrics` updates Quick Launch metrics.
- [x] Saving a preset captures current shared flag state.
- [x] Loading a preset applies shared flag state to Configure, Quick Launch, Chat sampler controls, and command preview.
- [x] Importing a preset stores normalized shared flag state.
- [x] Exporting a preset includes the shared flag state from `collectFlagValues()`.
- [x] Static syntax check passes for `ui/js/flags.js`.
- [x] Static syntax check passes for `ui/js/app.js`.

Phase 0 verification notes:

- Browser smoke used the local app at `http://127.0.0.1:5240`.
- Quick Launch context, Configure context, Quick Launch GPU, Configure GPU, template sync, metrics sync, preset save, and preset load were verified through live UI interactions.
- Quick Launch sampler sync was verified by loading the built-in Precise sampler preset from Quick Launch, which exercises the same grouped shared-state update path used by sampler controls.
- Chat sampler slider sync was verified from the existing event path in `ui/js/app.js`: `CHAT_SAMPLER_SLIDER_MAP` listeners call `setFlagValue()`, which runs the shared refresh path. The in-app browser driver did not move range inputs reliably with keyboard events.
- Preset import storage shape was verified through the local preset API using the same normalized `{ tool, model, flags }` payload shape produced by the frontend import handler.
- Preset export was verified by source path: `exportPreset()` normalizes preset data and serializes `{ tool, model, flags }`; the in-app browser does not support download capture, so the downloaded file contents were not inspected through browser automation.
- Temporary smoke presets `codex-phase0-smoke` and `codex-phase0-import-smoke` were deleted after verification.

## Phase 1: Create A Flag State And Launch Core Boundary

Status: Complete

Create a dedicated shared runtime boundary, initially compatible with classic script loading and global access.

Recommended file: `ui/js/flag-core.js`

The flag core should own:

- [x] `currentTool`
- [x] `flagValues`
- [x] `setCurrentTool`
- [x] `setFlagValue`
- [x] `setPathFlagValue`
- [x] `setMultipleFlagValues`
- [x] `collectFlagValues`
- [x] `applyFlagValues`
- [x] `shouldOmitFlagValue`
- [x] `getLaunchArgs`
- [x] `updateCommandPreview`

Implementation constraints:

- [x] Expose the API through `window.LlamaGui.flagCore`.
- [x] Keep compatibility shims for old global function names until all callers are migrated.
- [x] Keep all launch args generated from shared state, not DOM-local copies.
- [x] Keep `setMultipleFlagValues()` as the single write path for grouped flag updates.
- [x] Keep one post-update refresh hook that updates mirrored UI and command preview.

Expected result:

- `app.js` still orchestrates the app, but shared flag state and launch argument generation have one obvious home.

Phase 1 progress notes:

- Added `ui/js/flag-core.js` as the `window.LlamaGui.flagCore` namespace.
- Moved ownership of `currentTool`, `flagValues`, and `collectFlagValues()` cloning into the flag core.
- Added compatibility accessors so existing global `currentTool` and `flagValues` reads/writes continue to work during migration.
- Kept old global function names as wrappers so existing callers continue to work while new callers can target `window.LlamaGui.flagCore`.
- Browser smoke after this slice confirmed `flagCore` availability, Quick Launch context sync to Configure, command preview updates, and Quick Launch custom GPU sync into shared state and command preview.
- Moved `setFlagValue()`, `setPathFlagValue()`, and `setMultipleFlagValues()` into `flag-core.js`; `app.js` now provides small hooks for tab-specific side state and keeps old global wrappers for compatibility.
- Moved `setCurrentTool()` into `flag-core.js`; `app.js` now provides the DOM/render hook and keeps the old global wrapper for compatibility.
- Moved `applyFlagValues()` into `flag-core.js`; `app.js` now provides an apply hook for chat-template dropdown mapping and Quick Launch fit-link side state.
- Moved `shouldOmitFlagValue()` and `getLaunchArgs()` into `flag-core.js`; `app.js` now injects `FLAGS`, multi-enum normalization, compatibility filters, and the current model selector value.
- Moved `updateCommandPreview()` into `flag-core.js`; `app.js` now injects binary-name lookup and a render hook for writing the preview and refreshing related panels.
- Added selected-model ownership to `flag-core.js` so `getLaunchArgs()` reads the launch model from shared state instead of the Configure dropdown.
- Phase 1 browser smoke confirmed core API availability, Quick Launch context sync to Configure, custom GPU sync to command preview, selected model sync to Quick Launch, and launch args generated from core-owned selected model state.

## Phase 2: Add Flag Definition Validation

Status: Complete

Add lightweight validation around `ui/js/flags.js` so common llama.cpp flag update mistakes are caught early.

Recommended file: `ui/js/flag-validation.js`

Validation should check:

- [x] every flag has a unique `id`
- [x] every flag has a valid `category`
- [x] every flag has a valid `tool`: `server`, `cli`, or `both`
- [x] every flag has a supported `type`
- [x] `enum` and `multi_enum` flags have usable `options`
- [x] `false_flag` only appears on `bool` flags
- [x] default values are compatible with the declared type where practical
- [x] duplicate CLI flag strings are reported unless intentionally allowed

Implementation constraints:

- [x] Run validation once during frontend startup.
- [x] Report validation problems to the browser console.
- [x] Do not block app startup in the first version.
- [x] Avoid network access or upstream llama.cpp checks in this phase.

Phase 2 progress notes:

- Added `ui/js/flag-validation.js` as `window.LlamaGui.flagValidation`.
- Validation runs once after `flags.js` loads and reports to the browser console.
- Validation is non-blocking and returns structured `{ errors, warnings }` results for future tests or tooling.
- Phase 2 browser smoke confirmed the validator loads, app startup continues, and the current `FLAGS` table reports 0 errors and 0 warnings.

Expected result:

- Updating `flags.js` becomes safer without changing runtime behavior.

## Phase 3: Extract Configure Flag Rendering

Status: Complete

Move Configure tab flag rendering into a focused module after the shared flag core is stable.

Recommended file: `ui/js/config-flags-ui.js`

This module should own:

- [x] `renderFlags`
- [x] `createSubmenuBlock`
- [x] `createFlagRow`
- [x] Configure search filtering
- [x] expand/collapse behavior
- [x] `restoreFlagInputs`
- [x] `normalizeMultiEnumValue`
- [x] high-risk multi_enum warning display

Split `createFlagRow()` into type-specific builders:

- [x] bool row builder
- [x] enum row builder
- [x] multi_enum row builder
- [x] path row builder
- [x] int row builder
- [x] float row builder
- [x] text row builder

Implementation constraints:

- [x] All controls must read current values from `window.LlamaGui.flagCore`.
- [x] All controls must write through the shared setters.
- [x] No Configure control may keep a separate copy of launch-relevant state.
- [x] Search and expand/collapse state may remain local to the Configure UI module.

Phase 3 progress notes:

- Added `ui/js/config-flags-ui.js` as the `window.LlamaGui.configFlagsUi` namespace.
- Moved Configure rendering, search, expand/collapse, submenu state, input restoration, multi-enum normalization, and high-risk warning display into the Configure UI module.
- Split `createFlagRow()` into focused builders for bool, enum, multi_enum, path, int, float, and text inputs.
- Kept compatibility wrappers in `ui/js/app.js` so existing callers continue to use `renderFlags()`, `restoreFlagInputs()`, and related helper names while the implementation lives in the new module.
- Configure controls now read current values from `window.LlamaGui.flagCore` and write through `flagCore.setFlagValue()` / `flagCore.setPathFlagValue()`, preserving shared-state sync.
- Phase 3 browser smoke confirmed Configure renders all existing input types, search empty/clear behavior works, expand/collapse state works, `ctx_size` syncs to Quick Launch and command preview, `metrics` syncs to Quick Launch, and high-risk multi_enum warnings still display.

Expected result:

- Adding or tweaking a flag definition should usually require changing only `flags.js`.
- Adding support for a new flag input type should have one clear rendering location.

## Phase 4: Migrate Callers And Remove Compatibility Shims

Status: Complete

Once the flag core and Configure renderer are stable, migrate direct references in the remaining frontend code.

Areas to update:

- [x] Quick Launch controls
- [x] Chat sampler controls
- [x] sampler preset apply/load/save behavior
- [x] preset import/export behavior
- [x] launch/stop flow
- [x] API URL/model helpers
- [x] stats and metrics helpers that read host/port

Implementation constraints:

- [x] Migrate one area at a time.
- [x] Run the smoke checklist after each area.
- [x] Remove old global compatibility shims only after no callers remain.
- [x] Use Windows-safe `rg` patterns during migration checks:
  - Prefer `rg -n "pattern" ui/js`
  - Prefer `rg -n -g "*.js" "pattern" ui/js`
  - Avoid path globs like `rg "pattern" ui/js/*.js` because PowerShell passes them through in a way that causes `os error 123` on this system.

Phase 4 progress notes:

- Added the Windows-safe `rg` note after confirming `rg` itself works, but PowerShell path globs like `ui/js/*.js` fail with `os error 123`.
- Migrated Quick Launch controls to call `window.LlamaGui.flagCore` directly for tool mode, grouped profile/context writes, GPU layers, fit settings, sampler field writes, metrics, and refresh reads.
- Kept compatibility wrappers in place for the remaining Phase 4 areas.
- Quick Launch browser smoke confirmed context, GPU layers, metrics, tool mode, and sampler temperature still sync to Configure/shared state and update the command preview.
- Migrated Chat sampler controls to read and write through `window.LlamaGui.flagCore`.
- Chat sampler browser smoke confirmed temperature and max-token sliders sync to shared state, Configure, Quick Launch where applicable, and sidebar display labels. It also confirmed core-driven `top_p` updates refresh the Chat slider and label.
- Migrated sampler preset collection/application helpers to read and write through `window.LlamaGui.flagCore`.
- Sampler preset browser smoke confirmed built-in preset load, custom preset save, cross-panel preset-list refresh between Quick Launch and Configure, and custom preset load back into shared state, Configure, Quick Launch, and Chat sampler display labels. The native Chat temperature range input may round the slider thumb to its step value while the shared state and displayed label preserve the exact preset value.
- Migrated preset library save/update/export/import/load behavior in `ui/js/presets.js` to use `window.LlamaGui.flagCore` directly for shared tool, selected model, flag collection, and flag application.
- Preset import/export browser smoke used a page-level preset API stub and confirmed save captures shared flag state, export JSON matches normalized saved state, import stores the normalized `{ tool, model, flags }` shape, and load applies through `flagCore` with Quick Launch context and command preview refreshed.
- Migrated launch and restore-running flows to use `window.LlamaGui.flagCore` directly for launch args, current tool, host/port patching, and launch-time address output.
- Launch/stop browser smoke confirmed launch payloads use the core tool and shared args, restore-running updates the core tool and host/port state, and stop restores the expected UI state.
- Migrated API URL/model helpers, Chat API URL/model helpers, tool select initialization, and remote tunnel start payloads to read through `window.LlamaGui.flagCore`.
- API helper browser smoke confirmed server previews, Quick Launch server preview, API base URL, snippet model name, Chat API URL/model name, API key status note, selected-model fallback, and remote tunnel start payload all reflect core-owned state.
- Migrated stats polling to read metrics host/port through `window.LlamaGui.flagCore`.
- Removed the old app-level compatibility shims for global `currentTool`, global `flagValues`, and wrapper functions such as `setFlagValue()`, `applyFlagValues()`, `renderFlags()`, and `updateCommandPreview()` after confirming remaining callers use `flagCore` or `configFlagsUi` directly.
- Phase 4 final browser smoke confirmed metrics polling uses core host/port, stats values render correctly, command preview and Quick Launch context still refresh from core state, and the old global state shims are no longer present.

Expected result:

- The shared flag core becomes the single supported API for flag reads, writes, and launch args.

## Phase 5: Focused Smoke Coverage And Slider Precision

Status: Complete

- [x] Add Playwright smoke tests for mirrored controls and command preview.
- [x] Change the Chat temperature range input to `step="0.01"` so the slider thumb matches exact two-decimal sampler preset values such as `0.31`.

Phase 5 progress notes:

- Updated the Chat temperature slider to use `step="0.01"` so two-decimal sampler preset values can be represented exactly by the native range control.
- Added `tests/frontend/flag_sync_smoke.cjs`, a focused Playwright smoke script that serves `ui/`, stubs backend API calls, and verifies Quick Launch, Configure, Chat sampler, shared flag state, and command preview stay synchronized.

## Acceptance Criteria

- [ ] `flags.js` remains the single source of truth for exposed llama.cpp flags.
- [x] `flagValues` has one owner.
- [x] Every duplicated control updates through the shared setter path.
- [x] Command preview and launch arguments are generated from shared state only.
- [x] Configure rendering is isolated from Quick Launch and Chat behavior.
- [ ] New or renamed llama.cpp flags can be added with minimal changes outside `flags.js`.
- [ ] Existing user-facing behavior remains unchanged unless intentionally documented.
